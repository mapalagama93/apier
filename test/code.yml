name: Get user

preAction: 
  script: |
    import functions
    print("hello world", context.this['request']['url'])
    functions.do()

postAction: 
  script:
    file: scripts/post.py


request:
  method: get
  url: "{{v::server}}/{{api}}/users/{{user}}"
  pathParam:
    user: 2
    api: api
  queryParam:
    bla: "hello?/%heloasd"
    do: "&asdpopasd"
assign:
  fromBody:
    user_id: "$.data.id"
    user_email : "$.data.email"
  fromHeaders:
    date: "date"