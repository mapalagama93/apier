name: One
request:
  requestType: json
  responseType: json
  method: get
  url: "{{server}}/api/movies/1"
  headers:
    testheader1 : value1
    testheader2 : value2
assign:
  body:
    body_var_id: "$.id"
    body_var_id: "$.123"
    body_var_movie: "$.movie"
  headers:
    header_var: X-Nf-Request-Id
    header_var_not: X-Nf-Request-Idasd